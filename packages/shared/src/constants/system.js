/**
 * 系统级共享常量
 * 当前阶段先提供工程基础标识，后续逐步补充业务常量
 */

/**
 * 工作区名称常量
 * 后续前后端都可以复用
 */
const WORKSPACE_NAME = 'node-vmq';

/**
 * 当前已完成的任务包标识
 */
const CURRENT_STAGE = 'TP-11';

module.exports = {
  WORKSPACE_NAME,
  CURRENT_STAGE
};
